export class Sport {
    name: string;
    encounterPlayerCount: string;

    constructor(name: string, encounterPlayerCount: string) {
        this.name = name;
        this.encounterPlayerCount = encounterPlayerCount;
    }
}
